const config = {
  appName: 'Travl.bg',
  siteTitle: 'Travl.bg',
  defaultLanguage: 'bg',
  availableLanguages: ['bg', 'en'],
  contentLanguageMap: { bg: 'bg-BG', en: 'en-US' },
};

export default {
  ...config,
};
