query Travellog($id: ID) {
  travellog(_id: $id) {
    _id
    title
    author {
      firstName
      lastName
      email
      avatar
    }
    short_description
    html_content
    plain_text_content
    where
    duration
    price
    category
    season
    is_draft
    created_at
    updated_at
  }
}
